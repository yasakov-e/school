<%= render 'course-header' %>

<%= render 'teacher-journal' if current_user.teacher? %>

<%= render 'student-journal' if current_user.student? %>

<%= link_to t(:back), courses_path, class: 'btn btn-info back-btn' %>
